function [ Fx_adapt ] = useBelief( Vx, Vy, sspace )
% USEBELIEF returns the adaptation force (the force applied by the subject)
% INPUTS: states (Vx, Vy), State space
% OUTPUT: Adaptation Force

% steps:
%
% 1) Find the force distribution for the given state (Vx, Vy)
% 2) Sample from this distribution to finc 
%
%
%
%


end

